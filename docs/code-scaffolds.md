# Code Scaffolds – JavaScript / TypeScript

> Auto-generated by `/wf init`. Common code patterns for JavaScript / TypeScript projects.
> Use these as starting points when creating new files. Replace `{Name}` / `{name}` with your actual names.

---

## Express Router

Express.js router module

```
const express = require('express');
const router = express.Router();

router.get('/{name}', async (req, res) => {
  try {
    // TODO: implement
    res.json({ ok: true });
  } catch (err) {
    res.status(500).json({ error: err.message });
  }
});

module.exports = router;
```

---

## Service Class

Service layer abstraction

```
class {Name}Service {
  constructor(deps) {
    this.repo = deps.repo;
  }

  async getAll() {
    return this.repo.findAll();
  }

  async getById(id) {
    const item = await this.repo.findById(id);
    if (!item) throw new Error(`{Name} not found: ${id}`);
    return item;
  }
}

module.exports = { {Name}Service };
```

---
