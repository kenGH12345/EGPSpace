# F 阶段 · Code Diff 摘要

> Session: wf-20260429132738. · 基线 e12560f

## 核心变更（物理分层）

### 新建目录
- `src/lib/framework/contracts/`（9 文件 + index.ts）
- `src/lib/framework/runtime/`（5 文件 + index.ts）
- `src/lib/framework/builders/`（1 文件 + index.ts）

### 删除目录
- `src/lib/framework/components/`（5 文件迁走，目录删除）
- `src/lib/framework/solvers/`（1 文件迁走，目录删除）
- `src/lib/framework/interactions/`（3 文件迁走，目录删除）
- `src/lib/framework/assembly/`（7 文件迁走，目录删除）

### 15 核心文件迁移表（git mv）

| 原位置 | 新位置 | 性质 |
|--------|--------|------|
| `components/base.ts` | `contracts/component.ts` | co-located type+class |
| `components/graph.ts` | `contracts/graph.ts` | co-located type+class |
| `components/port.ts` | `contracts/port.ts` | type+helper fn |
| `components/union-find.ts` | `runtime/union-find.ts` | pure impl |
| `components/registry.ts` | `runtime/registry.ts` | stateful const |
| `solvers/base.ts` | `contracts/solver.ts` | type-dominant |
| `interactions/rule.ts` | `contracts/rule.ts` | pure type |
| `interactions/events.ts` | `contracts/events.ts` | type+const |
| `interactions/engine.ts` | `runtime/engine.ts` | impl-dominant |
| `assembly/spec.ts` | `contracts/assembly.ts` | type-dominant |
| `assembly/layout.ts` | `contracts/layout.ts` | type-dominant |
| `assembly/errors.ts` | `contracts/errors.ts` | type-dominant |
| `assembly/validator.ts` | `runtime/validator.ts` | impl-dominant |
| `assembly/assembler.ts` | `runtime/assembler.ts` | impl-dominant |
| `assembly/fluent.ts` | `builders/fluent.ts` | user-facing DSL |

### barrel 更新
- `src/lib/framework/index.ts` · 全部 re-export 路径更新（保持对外 API 100% 兼容）
- `src/lib/framework/contracts/index.ts` 新建聚合
- `src/lib/framework/runtime/index.ts` 新建聚合
- `src/lib/framework/builders/index.ts` 新建聚合

### import 路径级联更新（regex 批处理）
- `domains/chemistry/*.ts` + `reactions/*.ts` + `assembly/*.ts`（~10 文件）
- `domains/circuit/*.ts` + `assembly/*.ts`（~5 文件）
- 内部 framework 文件相对路径（~6 文件）
- 测试文件（2 处 fs path 修正）

## 硬约束兑现四件套

| # | 文件 | 作用 |
|---|------|------|
| 1 | `scripts/arch-audit.sh`（新建）| 4 依赖方向检查（零新依赖，纯 node）|
| 2 | `eslint.config.mjs`（追加 F 阶段 rule block）| no-restricted-imports · 外部 barrel-only |
| 3 | `docs/architecture-constraints.md`（更新）| E 条款归档 + F 物理边界写入 + 使用记录 #7 |
| 4 | `.workflow/skills/framework-boundary.md`（新建）| 100+ 行 · 未来 /wf Agent 必读 |

## 测试更新

- `framework/__tests__/layout-spec.test.ts` · AC-D1 fs path `assembly/layout.ts` → `contracts/layout.ts`
- `framework/__tests__/assembly.test.ts` · AC-A/AC-D 扫描目录 `assembly/` → `{contracts, runtime, builders}`（AC-A 排除 contracts 合理豁免 ComponentDomain union）

## E 阶段遗留修复（附带清理）

- `layout-spec.test.ts` · 删除 `/s` regex flag（TS1501 in target < es2018）
- `type-guards.test.ts` · `shape: 'round'` → `'beaker'`（FlaskShape enum）

## 验证结果

- **TSC**: 0 errors ✅
- **Jest**: 563/563 pass ✅（与 E 阶段基线等价）
- **arch-audit.sh**: 4 checks pass ✅（含 1 明示例外 graph.ts → union-find）
- **ESLint**: 0 errors, 0 warnings ✅
- **物理目录**: builders/ contracts/ domains/ runtime/ ✅
- **无新 npm 依赖** ✅

## 统计

- **迁移文件**: 15 核心 + 4 new barrels = 19
- **import 批处理**: ~25 文件（regex 批量 + 手工补漏）
- **新建文件**: 7（3 barrels + arch-audit.sh + constraints 更新 + framework-boundary.md + eslint 规则段）
- **删除文件**: 1（assembly/index.ts 孤儿 barrel）
- **删除目录**: 4（components/ solvers/ interactions/ assembly/）
- **工时**: ~90min（vs 预估 5h · 批处理 regex 大幅提速）

## PLAN_DEVIATION 记录

- **[T-0]** E 阶段遗留 3 tsc errors（regex `/s` flag + FlaskShape 字面量）就地修，不增任务
- **[T-6 amendment]** 同目录兄弟文件的相对 import 也要改（不仅 barrel + domains），W2/W3/W4 均遇到
- **[T-21 early cleanup]** W5 末尾一次性删 4 旧目录，未单开任务
- **[T-25 降级]** engines/ 确认无 bypass 需改（ANALYSE 假设修正）
- **[T-27 allowlist]** arch-audit 增加 `contracts/graph.ts → runtime/union-find` 明示例外

5 处 deviation，全部文档化 · 未触发 amendment cap。
