# DEVELOP Stage Output — Triple-Lock Architecture Implementation

> Session: `wf-20260424091211.`
> Stage: 5/7 DEVELOP
> Upstream: `output/execution-plan.md` (13 tasks / 4 Slices)
> Date: 2026-04-24

---

## 🧠 思考摘要 / Development Reasoning

| Q | A |
|---|---|
| Q1: 本轮实现目标? | 完成 Slice-0 + Slice-1 + Slice-2（除 T-7 集成测试外的所有 CODE 工作） |
| Q2: 最小变更原则如何贯彻? | 所有新功能通过**新增文件**实现；对现有文件的修改限于必需的集成点（3 个文件：page.tsx、route.ts、template-registry.ts） |
| Q3: 关键决策点? | (1) registry 与 routing 分离两个文件，符合 SRP；(2) postMessage schema 独立成库，便于单元测试；(3) 改造 API 时保留旧响应字段（schema）作为降级路径，仅**追加** templateId 字段 |
| Q4: 最大风险点及缓解? | T-2 postMessage 安全（origin+schema+source iframe 三重校验）；T-6 API 改造（保留兼容字段，灰度安全） |
| Q5: 未实现的部分? | T-7 集成测试（作为 TEST 阶段任务）；`src/components/__tests__/IframeExperiment.test.tsx` 单元测试（留到 TEST 阶段补齐） |

---

## ✅ 任务完成情况

| Task | 描述 | 状态 | 交付物 |
|------|------|------|--------|
| T-0 | 合规门（.gitignore + eslint） | ✅ 已存在（前序工作） | .gitignore, eslint.config.mjs |
| T-1 | 模板注册表 + 概念路由 | ✅ 完成 | template-registry.ts, concept-to-template.ts |
| T-1.5 | 共享样式 + postMessage 协议 | ✅ 完成 | ui-core.css, physics-core.js, experiment-message-schema.ts |
| T-2 | IframeExperiment 组件 🔥 | ✅ 完成 | IframeExperiment.tsx |
| T-3 | 浮力 HTML 模板 🎯 | ✅ 完成 | buoyancy.html (470+ 行) |
| T-3.5 | 浮力审核 | ✅ 完成 | physics-buoyancy.md |
| T-4 | 杠杆 HTML 模板 | ✅ 完成 | lever.html |
| T-4.5 | 杠杆审核 | ✅ 完成 | physics-lever.md |
| T-5a | 折射 HTML 模板 | ✅ 完成 | refraction.html |
| T-5a.5 | 折射审核 | ✅ 完成 | physics-refraction.md |
| T-5b | 电路 HTML 模板 | ✅ 完成 | circuit.html |
| T-5b.5 | 电路审核 | ✅ 完成 | physics-circuit.md |
| T-5c | 实验页面集成 | ✅ 完成 | page.tsx 改造 |
| T-6 | API 三重锁改造 🔥 | ✅ 完成 | route.ts 改造（Prompt 禁令+路由+净化） |
| T-7 | 集成测试 | ⏳ 留给 TEST 阶段 | — |

---

## 📝 Modified/Created Files

### 新增文件（14 个）

| # | 路径 | 规模 | 用途 |
|---|------|------|------|
| 1 | `src/lib/template-registry.ts` | ~200 行 | **Lock 3**：模板白名单注册表 + getTemplate/listApprovedTemplates API |
| 2 | `src/lib/concept-to-template.ts` | ~100 行 | **Lock 2**：概念关键字到模板 ID 的路由映射 |
| 3 | `src/lib/experiment-message-schema.ts` | ~180 行 | postMessage 通信契约 + validateIncomingMessage 白名单 |
| 4 | `src/components/IframeExperiment.tsx` | ~220 行 | iframe 容器 + origin/source/schema 三重校验 + skeleton UI + 错误边界 |
| 5 | `public/templates/_shared/ui-core.css` | ~230 行 | 所有模板共享 CSS 变量 + UI 组件样式 |
| 6 | `public/templates/_shared/physics-core.js` | ~200 行 | EurekaHost/EurekaCanvas/EurekaHints/EurekaFormat 运行时库 |
| 7 | `public/templates/physics/buoyancy.html` | ~470 行 | 浮力实验模板（物理计算+Canvas+拖拽+波动/气泡动画） |
| 8 | `public/templates/physics/lever.html` | ~220 行 | 杠杆实验模板（力矩平衡+倾斜动画） |
| 9 | `public/templates/physics/refraction.html` | ~270 行 | 折射实验模板（斯涅尔定律+全反射判断） |
| 10 | `public/templates/physics/circuit.html` | ~320 行 | 电路实验模板（串联/并联切换+电流流动动画） |
| 11 | `docs/audits/physics-buoyancy.md` | ~80 行 | 浮力审核报告（8 组边界值测试） |
| 12 | `docs/audits/physics-lever.md` | ~40 行 | 杠杆审核报告 |
| 13 | `docs/audits/physics-refraction.md` | ~45 行 | 折射审核报告（8 组边界值测试） |
| 14 | `docs/audits/physics-circuit.md` | ~50 行 | 电路审核报告（7 组边界值测试） |

### 修改文件（2 个）

| # | 路径 | 修改点 |
|---|------|--------|
| 1 | `src/app/experiments/[id]/page.tsx` | (a) 追加 3 个 import（IframeExperiment, conceptToTemplateId, getTemplate, enrichSchema）；(b) experiments 映射新增 templateId 字段；(c) renderExperiment 新增 resolvedTemplateId 逻辑，优先 iframe；(d) AI fetch 成功时记录 templateId 到 schema 上；(e) 移除未使用的 createBuoyancyRules import；(f) 将 let rules 改为 const rules（lint 修复）；(g) require() 改为顶部静态 import（lint 修复） |
| 2 | `src/app/api/generate/route.ts` | (a) 追加 conceptToTemplateId/getTemplate 导入；(b) System Prompt 新增**🚫 严格禁止**章节（Lock 1）；(c) 新增 sanitizeLLMOutput 函数（防御性净化）；(d) POST 响应新增 templateId 字段（Lock 2+3）|

---

## 🔒 三重锁架构落地验证

### Lock 1: Prompt 禁令（LLM 层）
- 位置：`src/app/api/generate/route.ts` buildSystemPrompt 函数
- 禁令内容：明确列出不允许输出的字段（canvas/interactions/html/css/js/shapes/elements）
- 防御加固：服务端 `sanitizeLLMOutput()` 函数即使 LLM 越权也会删除相关字段

### Lock 2: 路由分发（概念→模板）
- 位置：`src/lib/concept-to-template.ts` 中的 `CONCEPT_MAPPINGS` 数组
- 策略：多关键字匹配（中/英），最具体的先匹配
- 集成点：
  - 后端 `/api/generate` 路由时调用
  - 前端 `page.tsx` 的 `resolvedTemplateId` 计算时调用
  - 前端优先使用后端返回的 templateId（避免重复计算）

### Lock 3: 白名单校验（审核状态）
- 位置：`src/lib/template-registry.ts` 的 `getTemplate()` 函数
- 规则：只返回 `auditStatus === 'approved'` 的模板
- 已审核模板：`physics/buoyancy`、`physics/lever`、`physics/refraction`、`physics/circuit`（4 个）
- 审核文档：`docs/audits/physics-*.md`（4 份）

### 消息安全三重校验（IframeExperiment）
1. `event.origin === window.location.origin`（阻止跨 origin 攻击）
2. `event.source === iframe.contentWindow`（确保消息来自本 iframe）
3. `validateIncomingMessage()` 白名单 type + shape（阻止未知命令）
4. `templateId` 必须与当前 iframe 一致（阻止 iframe 伪冒）
5. iframe `sandbox="allow-scripts allow-same-origin"` + `referrerPolicy="no-referrer"`

---

## 🎯 Code 自检清单（Self-Review）

| ID | 检查项 | 结果 | 备注 |
|----|--------|------|------|
| SYNTAX-001 | 所有文件语法正确 | ✅ PASS | `npx tsc --noEmit` 零错误 |
| SEC-002 | 无硬编码 secret | ✅ PASS | 无任何凭据 |
| SEC-003 | 用户输入已校验 | ✅ PASS | templateId 必过白名单；postMessage 经多重过滤 |
| ERR-001 | async 操作有错误处理 | ✅ PASS | IframeExperiment 有 load-timeout fallback + error 状态 |
| REQ-001 | 所有验收准则反映在 diff 中 | ✅ PASS | T-0 至 T-6 的 AC 均已实现 |
| EDGE-001 | null/undefined 输入处理 | ✅ PASS | getTemplate/conceptToTemplateId 接受 null 返回 null |
| EDGE-002 | 空集合处理 | ✅ PASS | listApprovedTemplates 空结果返回 [] |
| EXPORT-001 | 导出符号与消费者匹配 | ✅ PASS | 验证过 page.tsx/route.ts 消费 |
| STYLE-002 | 无冗余注释 | ✅ PASS | 仅在公共 API 和非显而易见处保留解释性注释 |

---

## 🔥 Adversarial Code Review（攻击性自审）

### 1. Attack Surface: postMessage 攻击路径
**攻击场景**：恶意第三方脚本试图伪造 `postMessage` 向 host 发送假的 `result_update` 消息以污染数据。
**防御**：
- `event.origin === window.location.origin` 阻断任何跨域来源
- `event.source === iframe.contentWindow` 确保只有本 iframe 的消息被接受
- `validateIncomingMessage()` 白名单 type 拒绝未知消息
- `templateId` 强制匹配阻止 iframe 间冒充

### 2. Regression Risk
- **AI 生成实验流程**：保留了旧的 schema + Canvas 渲染作为降级路径。如果 concept 没匹配到 templateId，仍然走 UniversalPhysicsRenderer。**无回归**。
- **预设实验**：页面先查 `experiment.templateId`，命中则走 iframe，否则走原先的 `BuoyancyExperiment`/`*Placeholder`。**无回归**。

### 3. Edge Case Stress
- `getTemplate(null)` → null（安全）
- `getTemplate('unknown/id')` → null（安全）
- `getTemplate('physics/buoyancy')` 在审核前返回 null，审核后返回元数据（正确）
- `conceptToTemplateId('')` → null（安全）
- iframe 加载 8s 超时 → 显示友好错误（已实现）
- 模板报 error → host 显示错误覆盖层（已实现）

### 4. Dependency Chain
本轮新增依赖：**0 个 npm 包**。所有功能基于现有栈（React、Next.js）+ 浏览器原生 API。无外部依赖风险。

---

## 📋 Verification Commands

```bash
# TypeScript 编译
npx tsc --noEmit
# 结果：✅ 零错误

# ESLint 检查（6 个新/改文件）
npx eslint src/lib/template-registry.ts src/lib/concept-to-template.ts \
  src/lib/experiment-message-schema.ts src/components/IframeExperiment.tsx \
  src/app/experiments/[id]/page.tsx src/app/api/generate/route.ts
# 结果：✅ 0 errors, 4 warnings（均为原遗留代码的未使用变量）

# Template 白名单审核状态
# - physics/buoyancy:   approved ✓
# - physics/lever:      approved ✓
# - physics/refraction: approved ✓
# - physics/circuit:    approved ✓
```

---

## 🚀 如何手动验证

1. 启动 dev server：`npm run dev`
2. 访问 `http://localhost:3000/experiments/buoyancy` — 应看到 iframe 加载的浮力实验
3. 打开浏览器 DevTools Console — 应看到 postMessage 通信日志（无 warning）
4. 移动参数滑块 — 应触发 param_change 消息，实验画面实时响应
5. 访问 `http://localhost:3000/experiments/lever` / `refraction` / `circuit` — 均走 iframe 路径
6. 访问 `http://localhost:3000/experiments/acid-base`（无模板）— 降级到 Placeholder 组件
7. 访问 `http://localhost:3000/experiments/combustion`（AI 生成）— 触发 /api/generate，无模板则降级 Canvas

---

**DEVELOP 阶段已完成。等待进入 TEST 阶段进行完整的 lint / test / cve / entropy 检查。**
